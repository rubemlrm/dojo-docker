version: '3.7'


networks:
  proxy_network:
    name: proxy_network
    external: true

services:
  couchdb:
    restart: always
    labels:
      - "traefik.enable=true"
      # The Traefik Network
      - "traefik.docker.network=proxy_network"
      - "traefik.http.routers.obsidian-livesync.rule=Host(`${DOMAIN}`)"
      - "traefik.http.routers.obsidian-livesync.entrypoints=${DEFAULT_ENDPOINT}"
      - "traefik.http.routers.obsidian-livesync.service=obsidian-livesync"
      - "traefik.http.services.obsidian-livesync.loadbalancer.server.port=5984"
      - "traefik.http.routers.obsidian-livesync.tls=${ENABLE_SSL}"
      - "traefik.http.routers.obsidian-livesync.tls.certresolver=letsencryptresolver"
      - "traefik.http.routers.obsidian-livesync.middlewares=obsidiancors"
      - "traefik.http.middlewares.obsidiancors.headers.accesscontrolallowmethods=GET,PUT,POST,HEAD,DELETE"
      - "traefik.http.middlewares.obsidiancors.headers.accesscontrolallowheaders=accept,authorization,content-type,origin,referer"
      - "traefik.http.middlewares.obsidiancors.headers.accesscontrolalloworiginlist=app://obsidian.md,capacitor://localhost,http://localhost"
      - "traefik.http.middlewares.obsidiancors.headers.accesscontrolmaxage=3600"
      - "traefik.http.middlewares.obsidiancors.headers.addvaryheader=true"
      - "traefik.http.middlewares.obsidiancors.headers.accessControlAllowCredentials=true"
      - 'com.centurylinklabs.watchtower.enable=true'
    image: couchdb:latest
    container_name: obsidian-livesync
    user: "${PUID}:${PGID}"
    environment:
      - COUCHDB_USER=${OBSIDIAN_USER}
      - COUCHDB_PASSWORD=${OBSIDIAN_PASSWORD}
    volumes:
      - ${DATA_PATH}/volumes/obsidian-sync:/opt/couchdb/data
      - ./local.ini:/opt/couchdb/etc/local.ini
    ports:
      - 5984:5984
    networks:
      - proxy_network
